Autor: Maksim Tikhonov (xtikho00)

Jazyk: Dart (Flutter framework)

Zdrojový kód: /BirthdayCalendar/lib

P.S. výše uvedený adresář obsahuje pouze zdrojový kod.
(nebylo možné umístit ani .apk soubor ani kompletní adresář s 
projektem kvůli omezením na výslednou velikost archivu.)

Pro instalace aplikace je poprve potřeba instalovat flutter sdk a flutter v rozšířeních VS codu.
Pak použit příkazy v adr. BirthdayCalendar

flutter pub get
flutter build sdk
